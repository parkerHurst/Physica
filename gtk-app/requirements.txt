# System packages required (install via package manager):
# - python3-gi (or python-gobject on Arch)
# - python3-dbus (or python-dbus on Arch)

# Note: PyGObject and dbus-python are installed via pip as fallback
# if system packages are not available
PyGObject>=3.42.0
dbus-python>=1.2.18

# Date/time handling
python-dateutil>=2.8.2
