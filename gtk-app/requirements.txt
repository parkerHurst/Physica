# System packages required (install via package manager):
# - python3-gi (or python-gobject on Arch)
# - python3-dbus (or python-dbus on Arch)
# - python3-cairo (or python-cairo on Arch)

# Note: These packages should be installed via system package manager
# to avoid build issues. Only install via pip if system packages unavailable.

# Date/time handling
python-dateutil>=2.8.2
